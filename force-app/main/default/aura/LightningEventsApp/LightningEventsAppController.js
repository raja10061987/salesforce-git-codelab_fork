({
    handleApplicationEvent : function(component, event, helper) {
        // let id = component.get('v.id');
        // console.log('id: ' + id);
        alert('application event in my application root: ');
       // component.set('v.message',event.getParam('message'));
    }
})